// Domain types/schemas shared between packages/web and packages/functions go here
// (e.g. student/account, behavior, reward, transaction shapes) as the product
// takes shape. Empty on purpose at scaffold time — there's no product schema yet.
export {};
